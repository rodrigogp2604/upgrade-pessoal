# Copie para .env. Caminho relativo ao schema.prisma (api/prisma/).
DATABASE_URL="file:../../data/upgrade.db"
PORT=4000
# UPLOAD_DIR="../../data/uploads"   # opcional; padrão já aponta para ../data/uploads
